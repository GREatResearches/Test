package fmp.contextModel;

public enum ContextType {
	//Optional => Optional Feature
	//Mandatory => Cannot be a mandatory Context
	//Group => indicates that it is a context feature from a Context Group
	Optional, Group;
}
