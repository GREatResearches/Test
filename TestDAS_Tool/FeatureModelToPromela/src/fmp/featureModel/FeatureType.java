package fmp.featureModel;

public enum FeatureType {
	//Optional => Optional Feature
	//Mandatory => Mantadory Feature
	//Group => indicates that it is a feature from a Featur Group
	Optional, Mandatory, Group;
}
